# Database Configuration
# PostgreSQL connection string
DATABASE_URL=postgresql://user:password@localhost:5432/cloudvault

# Session Configuration
# CRITICAL: Must be a strong random string (minimum 32 characters)
# Generate with: openssl rand -base64 32
# NEVER commit the actual secret to source control
SESSION_SECRET=your-session-secret-here-change-in-production

# Replit Authentication (OIDC)
# Your Replit application ID
REPL_ID=your-repl-id

# OIDC issuer URL (default: Replit)
ISSUER_URL=https://replit.com/oidc

# Server Configuration
# Port to run the server on (default: 5000)
PORT=5000

# Node environment (development, production, test)
NODE_ENV=development

# Google Cloud Storage (via Replit Object Storage)
# These are typically auto-configured on Replit platform
# GCS_BUCKET_NAME=your-bucket-name
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/credentials.json
